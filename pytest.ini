[pytest]
testpaths = tests
pythonpath = src
addopts =
    -v
    --tb=short
    --cov=handlers
    --cov=services
    --cov=repositories
    --cov=api
    --cov-report=html:coverage
    --cov-config=.coveragerc
